# mistakes.md — 실수 대장 (자가 진화 기록)

형식: `YYYY-MM-DD | 증상 | 원인 | 일회성: 예/아니오 | 조치`
동일 원인 2회 반복 시 규칙화한다 (RULES.md §1). 규칙화된 항목에는 `→ 규칙화됨` 표시.

| # | 날짜 | 증상 | 원인 | 일회성 | 조치 |
|---|---|---|---|---|---|
| 1 | 2026-08-22 | CLAUDE.md/.claude/tools 등이 타 프로젝트(Board Provisioner·STOM·Persephone) 내용으로 채워져 있어 세션 규칙·훅이 오작동 소지 | 프로젝트 부트스트랩 시 다른 프로젝트의 에이전트 설정을 통째로 복사 | 예 | SerialTool 기준으로 전면 재작성. 새 프로젝트 시작 시 설정 파일은 복사하지 않고 프로젝트 파악 후 새로 작성한다 |
| 2 | 2026-08-22 | 마이그레이션이 제거·개명한 설정 키가 파일에 되살아남 (3회: ui 폰트 키 S-027, right_section_width S-028, serial.flowctrl S-030) | defaults(create_fallback_settings)가 옛 키를 계속 보유 — fallback-base 병합이 마이그레이션 결과를 덮음 | 아니오 | → 규칙화됨: S-030 Step 5에 기계적 차단 테스트(마이그레이션을 defaults에 적용하면 no-op이어야 함) 추가. 이후 마이그레이션 변경 시 defaults를 반드시 함께 갱신 |
| 3 | 2026-08-22 | 타 프로젝트(Board Provisioner) 문서 doc/ui-guidelines.md가 리뷰 없이 커밋에 딸려 들어감 (54cab4a) | 에이전트 병렬 작업 중 출처 불명 파일이 생겼는데 리뷰어가 `git add -A`로 스테이징 — RULES §5 pathspec 규율 위반 | 예(재발 시 규칙화) | 파일 제거 커밋. 다중 에이전트 세션에서는 커밋 직전 `git status` 확인 + pathspec `git add`만 사용. 후기: 파일 자체는 사용자가 의도적으로 넣은 원자료로 확인됨(사용자 지시로 참조 자료로 재추적, 적합화본은 .agent/rules/ui_guide.md) — 그러나 "리뷰 없이 커밋"이 문제였다는 교훈은 유효 |
| 7 | 2026-08-22 | S-058 커밋 직전 pytest 1회 실행에서 1건 실패(테스트명 미확인) — 직후 6회 연속 재현 실패. **[S-066 후속, 2026-08-22]** 이름 특정됨: `tests/test_shutdown_data_logger.py::TestShutdownStopsDataLogger` 내 `test_shutdown_stops_data_logger_and_preserves_written_bytes`/`test_shutdown_closes_pcap_with_valid_header_and_packet_structure` | 미확정 상태에서 S-066으로 조사·확정: `connection_controller.send_data()`가 발신(TX) 즉시 EventBus 경유로 `DataTrafficHandler.on_data_sent()`에도 라우팅되어(전이중/Full-Duplex 로깅, 기존 의도된 설계) 로거 큐에 곧바로 1회 기록된다. 두 테스트가 LOOPBACK 포트에 이 API로 payload를 보냈는데, LOOPBACK은 보낸 바이트가 그대로 에코되어 RX Fast Path로도 다시 들어온다. 종료(`on_close_requested`) 이전에 그 RX 배치까지 추가로 flush되는지 여부(워커 스레드 스케줄링에 좌우되는 경합)에 따라 파일에 payload가 1회(TX만 — 우연히 테스트 기대와 일치)와 2회(TX+RX — 테스트 실패) 사이를 오갔다. 부수적으로 두 테스트의 준비 조건(`dl._queue.empty()`만 확인)도 "아직 아무것도 안 옴"과 "다 써서 비었음"을 구분 못 해 조기 종료를 유발하는 별도 결함이었다. **판정: 테스트 문제 (프로덕션 유실 아님)** — 실제 종료 시퀀스(S-059/061/062)는 재현 시도(단독 파일 반복 250회+ In-process 300회) 전부에서 데이터를 온전히 보존했다 | 미확정 → 특정·해결됨(S-066) | S-066에서 형제 테스트(`test_shutdown_flushes_pending_leftover_batch_before_closing_logger`)가 이미 쓰던 "TX 경로 우회, `worker.transport.write()`로 직접 주입" 패턴을 두 테스트에도 적용해 순수 RX Fast Path만 재현하도록 수정. 준비 조건에 `dl._file.tell() >= 기대 최소 바이트` 확인 추가. 수정 후 전체 pytest 10회 반복 전부 통과, ruff 0건. → 규칙화됨: LOOPBACK 기반 테스트에서 `send_data()`로 RX Fast Path만 검증하려 하면 안 된다 — 반드시 `worker.transport.write()`로 TX 경로를 우회할 것(이미 한 테스트가 문서화했던 패턴을 나머지에도 일반화하지 못해 반복된 사례, #9와 같은 계열) |
| 6 | 2026-08-22 | 리팩토링 감사에서 만든 태스크 14건(S-038~S-051)이 `doc/refactor_audit_20260822.md`에만 있고 작업 보드(Task.MD·tasks/README.md)에 등재되지 않음 | 감사 보고서를 보드 대신 사용. Task.MD 운영 규칙("새 태스크는 이 표에 추가한다")을 상위가 스스로 어김 | 예(재발 시 규칙화) | 두 보드에 일괄 등재. 태스크 생성 시 보드 등재를 같은 커밋에 포함한다 |
| 5 | 2026-08-22 | S-049 수행자가 minimumSizeHint 실측 791을 "문서 기준선 780과 11px 차이"로 보고 — 회귀인지 확인하느라 stash 대조까지 수행 | S-035에서 최소 높이가 780→791로 바뀌었는데 상위(나)가 회귀 기준선으로 지정한 `.agent/rules/ui_guide.md` §4를 갱신하지 않음. 태스크 파일에만 기록 | 예(재발 시 규칙화) | ui_guide §4에 최신값·이력·"최소 크기 변경 시 이 줄을 함께 갱신" 지시 추가. task-done 스킬의 문서 갱신 항목에 UI 크기 기준선 추가 |
| 8 | 2026-08-22 | 태스크 상태가 태스크 파일·Task.MD·tasks/README.md에서 서로 다르게 기록됨(실측 30건). 감사 계열 19건은 보드만 완료·파일 헤더는 TODO였고, S-036은 Task.MD 안에서만 완료/대기로 두 번 등장 | 상태가 세 곳에 중복 기록되는데 task-done 스킬이 `Task.MD`만 갱신하라고 적혀 있었다. 나머지 두 곳은 절차에 언급조차 없어 수행자가 빠뜨려도 아무도 몰랐다 | 아니오 (#6과 같은 계열 — 보드 기록 신뢰성) | → 규칙화됨: ① 기계적 차단 `tools/check_task_boards.py`(3중 교차 검증, CI 게이트) ② task-done 스킬 §2를 "세 곳 모두" 명시로 개정 ③ CLAUDE.md 검증 명령에 추가. **부수 교훈**: 검사를 처음 돌렸을 때 64건이 떴으나 상당수가 내 파서 결함(비고 열을 상태로 오독, 괄호 안 부연의 "대기/보류"를 판정으로 오독)이었다 — 보드를 고치기 전에 검사부터 의심해 실드리프트 30건을 분리했다 |
| 9 | 2026-08-22 | S-063 태스크에 상위가 적은 대비 수치 일부가 실제 QSS에 없는 색 조합이었음 (warning을 `#ffffff` on `#FF9800` 2.16:1로 기재했으나 실제로는 `#FFC107` 배경 + 어두운 글씨로 이미 기준 충족). 하위가 실측 재확인 후 진짜 결함만 수정 | accent 한 건만 QSS로 확인하고 **danger/warning은 같은 패턴이려니 추정**해 계산했다. RULES §8 "Task 전제는 작성 시점에 코드로 확인"을 상위가 스스로 어김 — #4와 같은 원인의 3회차 | 아니오 (#4 재발) | → 규칙화됨: RULES §8에 "**부분 확인은 확인이 아니다** — Task에 수치를 N개 적으면 N개 모두 코드에서 읽는다. 한 건에서 본 패턴을 나머지에 일반화하지 않는다" 추가. 부수 효과로 하위가 재확인하며 상위가 놓쳤던 더 심한 결함(`warning:disabled` 1.22:1, `warning:pressed`가 danger 빨강)을 찾아냄 — 하위의 "전제 재확인" 규율이 또 한 번 오염을 막았다 |
| 4 | 2026-08-22 | 상위가 작성한 Task의 전제가 실제 코드와 달라 하위가 중단·재조사 (2회: S-030 "flowctrl→flow_control이 올바른 개명" 전제, S-032 "PortStats가 1열 5행" 전제) | Task 작성 시 코드를 실측하지 않고 기억·보고서 요약으로 추정 | 아니오 | → 규칙화됨: RULES §8에 "Task 전제는 작성 시점에 코드로 확인, 하위 Step 1은 전제 재확인+불일치 시 중단" 추가. 하위의 중단·보고 규율이 두 번 모두 오염을 막았음 (체계 정상 작동) |
